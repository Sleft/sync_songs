#!/usr/bin/env ruby
# -*- coding: utf-8; mode: ruby -*-

require 'thor'
require_relative '../lib/sync_songs.rb'

# Public: Classes for syncing sets of songs.
module SyncSongs
  class Cli < Thor
    class_option :verbose, type: :boolean, aliases: '-v', desc: 'Explain what is being done'
    services_option = [:services, {type: :array, required: true,
                         banner: Controller::INPUT_FORM,
                         desc: 'At least two users or file paths each paired with a service and a type, e.g. -s user1:grooveshark:favorites user1:lastfm:loved',
                         aliases: '-s'}]

    desc 'sync', 'Sync sets of songs'
    long_desc <<-LONGDESC
    WRITE!!!
   LONGDESC
    method_option(*services_option)
    def sync
      setupController
      @controller.sync
    end

    desc 'diff', 'Diff sets of songs'
    long_desc <<-LONGDESC
    #   WRITE!!!
     LONGDESC
    method_option(*services_option)
    def diff
      setupController
      @controller.diff
    end

    desc 'supp', 'List supported services'
    long_desc <<-LONGDESC
  Prints a list of the supported services.
     LONGDESC
    method_option(*services_option)
    def supp
      CLI.supportedServices
    end

    private

    def setupController
      @controller = Controller.new(CLI.new(options[:verbose]),
                                   options[:services])
    end
  end
  
  Cli.start(ARGV)
end
